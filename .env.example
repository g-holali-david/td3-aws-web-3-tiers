# Copie ce fichier en .env :  cp .env.example .env
#
# Plus besoin de mot de passe ici : les identifiants RDS proviennent du secret
# AWS Secrets Manager "td-ipssi-rds-v2/password" (lu par Terraform).
#
# (optionnel) surcharges :
# TF_VAR_aws_region=eu-west-3
